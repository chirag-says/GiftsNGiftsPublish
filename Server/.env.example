# ========================================
# GNG E-Commerce Server Environment Variables
# ========================================
# Copy this file to .env and fill in the values
# 
# ⚠️ SECURITY WARNING: Never commit .env files to version control!
# ⚠️ All secrets should be generated using cryptographically secure methods

# ========== SERVER CONFIGURATION ==========
NODE_ENV=development
# Options: development, production
# IMPORTANT: Set to 'production' in production for security features
PORT=7000

# ========== DATABASE ==========
mongodb_url=mongodb+srv://username:password@cluster.mongodb.net/database_name
# SECURITY: Use a strong, unique password. Enable MongoDB Atlas IP whitelisting.

# ========== JWT AUTHENTICATION ==========
JWT_SECRET=your_super_secure_jwt_secret_key_here
# SECURITY: Use at least 64 characters of random data
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# ========== CLOUDINARY (Image Upload) ==========
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ========== EMAIL CONFIGURATION ==========
SMTP_EMAIL=your_email@gmail.com
SMTP_PASS=your_email_app_password
SENDER_EMAIL=noreply@giftngifts.in

# ========== RAZORPAY PAYMENTS ==========
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# SECURITY: Webhook secret for signature verification
# Get this from Razorpay Dashboard > Settings > Webhooks > Your webhook
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_from_razorpay_dashboard

# ========== FRONTEND URLs (PRODUCTION CRITICAL) ==========
# Set this to your actual frontend URL in production
FRONTEND_URL=https://giftngifts.in

# ========== CORS CONFIGURATION ==========
# Comma-separated list of allowed origins (optional - defaults are used if not set)
# In production, don't include localhost origins
ALLOWED_ORIGINS=https://giftngifts.in,https://www.giftngifts.in

# Wildcard host suffixes for CORS
ALLOWED_HOST_SUFFIXES=ishisofttech.com,giftngifts.in

# ========== ADMIN CREDENTIALS ==========
# SECURITY: Use strong passwords with uppercase, lowercase, numbers, and symbols
ADMIN_EMAIL=admin@giftngifts.in
ADMIN_PASSWORD=your_secure_admin_password

# ========================================
# SECURITY CHECKLIST FOR PRODUCTION:
# ========================================
# 1. ✅ NODE_ENV=production (enables strict CORS, secure cookies)
# 2. ✅ Strong JWT_SECRET (64+ characters)
# 3. ✅ RAZORPAY_WEBHOOK_SECRET configured
# 4. ✅ HTTPS enabled (Secure cookies require HTTPS)
# 5. ✅ MongoDB Atlas IP whitelist configured
# 6. ✅ All test/development credentials removed
# ========================================
